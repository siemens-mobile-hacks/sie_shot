# SieShot – Native ELF Screenshot Daemon for Siemens Mobile Phones

SieShot is a native ELF daemon for Siemens mobile phones. It captures screenshots and saves them as PNG or JPEG files to a user‑defined folder.

## Building
The SDK must be located at `../sdk` relative to the project root.
```bash
mkdir build && cd build
cmake ..
make
```
